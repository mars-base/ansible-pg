---
cron_base_dir: "/srv/cron"
cron_secret_key_name: ".key"
cron_secret_key_file: "{{ cron_base_dir }}/{{ cron_secret_key_name }}"
cron_secret_key: "CHANGEME"

cron_log_dir: "{{ cron_base_dir }}/logs"
cron_scripts_dir: "{{ cron_base_dir }}/scripts"
